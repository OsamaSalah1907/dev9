import 'package:dev_login/Users_Tracks/Components/body.dart';
import 'package:flutter/material.dart';

class UsersTracksScreen extends StatelessWidget {
  const UsersTracksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(),
    );
  }
}